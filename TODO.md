# TODO

- [x] Render the Quarto site locally with `quarto render` after initializing sync files.
- [ ] Review generated files under `docs/` before committing because GitHub Pages serves from that directory.
- [ ] Keep profile/CV assets under `files/` and update `_quarto.yml` links when filenames change.
- [ ] Keep publication and news updates synchronized between `index.qmd` and `publications.qmd`.
- [ ] Preserve `_extensions/` unless intentionally upgrading Quarto extensions.
